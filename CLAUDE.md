# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Graph-builder is a Python project (requires Python 3.11+).

## Commands

```bash
# Run the application
python main.py

# Install dependencies (when added)
uv sync
```
